services:
  - type: web
    name: rjp-showcase-api
    runtime: python
    plan: free
    rootDir: .
    buildCommand: pip install -r requirements.txt && npm install
    startCommand: python3 scripts/showcase_api.py --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
